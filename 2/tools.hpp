#ifndef TOOLS_HPP
#define TOOLS_HPP

void specialFunc(int key, int x, int y);
void keyboardFunc(unsigned char key, int x, int y);
bool load_textures();
void render_scene();
void display(void);

#endif
